# CI/CD Pipelines

Add separate Terraform PR, Terraform apply, Terraform drift, and application build/promote pipelines here. The application pipeline must build once and promote the same image digest from Dev to Prod.

