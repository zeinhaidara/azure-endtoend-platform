# Remote State Bootstrap

Create the state storage account and private blob container manually, then record the exact procedure in `docs/bootstrap.md`. This is the only manual infrastructure exception.

